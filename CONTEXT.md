# CONTEXT.md — ClashUI 领域词汇

## 宿主编排（Hosting）
- **宿主（Host）**：应用进程的生命周期编排者，负责单例互斥、激活转发、提权门控与核心编排器的装配与启停。
- **单例守卫（SingleInstanceGuard）**：保证同一用户会话仅一个实例运行的互斥机制。
- **激活桥（ActivationBridge）**：第二实例向前台实例转发“显示面板”信号的进程间通道。

## 平台策略（Platform）
- **平台策略（PlatformPolicy）**：对操作系统侧副作用的统一收敛 — 系统代理、开机自启、TUN 提权重启的期望状态与实际状态的调和。
- **期望状态（DesiredState）**：调用方期望的平台侧状态快照（系统代理开关/端口、TUN 开关、开机自启、静默启动），按值传递。
- **调和（Reconcile）**：启动时对崩溃残留（如指向本应用端口的系统代理）与挂起的提权操作（Pending）的自动修复。

## 托盘呈现（Tray）
- **托盘呈现（Tray）**：常驻托盘图标与菜单的呈现层，负责图标四态（关闭/代理/TUN/正常）与配置文件的保序菜单。
- **托盘视图（TrayView）**：托盘呈现的视图抽象，接收视图模型并发出用户命令。
- **托盘视图模型（TrayViewModel）**：托盘呈现的纯数据快照，含图标、提示与菜单结构。

## 核心编排（Core）
- **核心编排器（CoreOrchestrator）**：mihomo 核心的生命周期与配置热重载的编排者。
- **核心运行时（CoreRuntime）**：mihomo 进程的托管与探活（进程启动、健康检查、崩溃重启、Job 绑定）。
- **配置合成（ConfigComposer）**：将用户订阅与应用注入项合成为核心实际加载的运行时配置。

## 通用
- **通知（Notification）**：面向用户的轻量提示，经由宿主或面板横幅展示。
